repos:
  - name: devteam
    path: /home/lobsterdog/source/devteam
    role: primary
    changes: |
      UI-only changes. Add KanbanBoard + KanbanColumn React components under ui/src/components/.
      Add a view-mode toggle to ui/src/pages/Dashboard.tsx with localStorage persistence.
      Reuse the existing FeatureCard (or a thin board-card variant sharing its badge logic) and
      the existing GET /api/features API client (no backend changes). Add Playwright E2E coverage
      in ui/e2e/app.spec.ts and unit tests for the phase-grouping / status-classification helpers.
      No Go changes, no database changes, no new API endpoints.