repos:
  - name: devteam
    path: .
    role: primary
    changes: >
      Frontend-only changes in ui/. Add a KanbanBoard component (and a
      KanbanCard variant or reused FeatureCard) under ui/src/components/.
      Add a List/Kanban view toggle to ui/src/pages/Dashboard.tsx with
      localStorage persistence. Add Playwright E2E specs under ui/e2e/ for
      the board. Add Vitest unit/integration tests for column grouping,
      unknown-phase handling, and localStorage fallbacks. No backend
      (internal/, cmd/) changes. No new npm dependencies (CON-007).